#include <cassert>
#include <cstring>
#include <cstdio>
#include <cstdint>
#include <new>

#include "MyRegex.h"

void RunTests();


// TUNE BM!!!!!!!!!!!!



int main()
{
   RunTests();
   


//    char* text = "The Boyer–Moore–Horspool algorithm is a simplification of the Boyer–Moore algorithm using only the bad character rule.";
//    char* pat = "algorithm";
//    char* pos = boyer_moore(text, strlen(text), pat, strlen(pat));
// 
//    printf("%s\n", pos);
//    
//    ++pos;
//    pos = boyer_moore(pos, strlen(pos), pat, strlen(pat));
// 
//    printf("%s\n", pos);
// 
// 
//    size_t q_mark_num = 0;
//    for(const char* pos = text; *pos; q_mark_num += (*pos++=='–'));
// 
//    printf("%d\n", q_mark_num);
   
   
}